<form method="post" action="{{ path('dashboard_assignment_type_delete', {'id': assignment_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assignment_type.id) }}">
    <button class="btn">Delete</button>
</form>
